import { EditableTitle } from './editable-title';

describe('EditableTitle', () => {
  it('should create an instance', () => {
    const directive = new EditableTitle();
    expect(directive).toBeTruthy();
  });
});
